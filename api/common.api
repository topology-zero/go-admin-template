syntax = "v1"

type (
    PathID {
        ID int `uri:"id"`
    }

    ParamID {
        ID int `form:"id"`
    }

    IDAndName {
        ID int `json:"id"`
        Name string `json:"name"`
    }
)
